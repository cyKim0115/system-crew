# docs/

| Path | Purpose |
|------|---------|
| `site/` | Public GitBook source (`SUMMARY.md` + pages). Sync project directory: `docs/site` |

Runtime protocol for Cursor agents remains at the repo root (`AGENTS.md`, `roles/`, `workflows/`).
